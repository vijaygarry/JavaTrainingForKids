Math Game Homework
------------------------
Create a Math game as follows:
Game first ask player name.
Then present the 10 problems to player one by one.
After presenting each problem record the answer from user.

Presenting problem to player as follows:
Generate 2 random numbers as num1 and num2. Select a randonm operation from operation list (+, -, *, /, or %).
Now show the question player like below:
           num1
     (op)  num2
------------------
Enter answer:

Once player done with all the answers, print the result as follows:

Sr.No.  Problem             User Answer     Actual Answer     Result
----------------------------------------------------------------------
1)      num1 (op) num2 =    UserAnswer      Actual answer     Right
2)      num1 (op) num2 =    UserAnswer      Actual Answer     Wrong
.
.
10)     num1 (op) num2 =    UserAnswer      Actual Answer     Wrong
----------------------------------------------------------------------
Total                                                          5/10


Hint:
//Returns a random, uniformly distributed int value between 0 (inclusive) and the specified value (exclusive),
int rnd = new Random().nextInt(5);
e.g.
int rndArrayIndex = new Random().nextInt(operationArray.length);

Random number for operation:
int rndNumber1 = new Random().nextInt(20);
int rndNumber2 = new Random().nextInt(20);


Solution:
----------------------------------------
Create a MathProblem class as follows:

import java.util.Random;

public class MathProblem {
 
  public static char[] operations = {'+', '-', '*', '/', '%'};
 
  private int num1;
  private int num2;
  private char operation;
  private int playerAnswer;
 
  public void generateQuestion() {
    // set random number for num1 and num2
    // set random operation name to operation
  }
 

  public void printProblem() {
    // Print the problem to user
  }

  public void recordAnswer(int aAnswer) {
    playerAnswer = aAnswer;
  }

  public int getAnswer () {
    // Calculate and return the answer
  }
 
 
  public boolean isRightAnswer() {
    // Return if user answer is same as calculated answer
  }
}







Create a MathGame as follows:


import java.util.Scanner;

public class MathGame {
  public static void main(String[] args) {
   
    System.out.println("\t\tWelcome to math game");
    String playerName = readUserStringInput("Enter player name:");
    MathProblem[] problems = new MathProblem[10];
    for(int i =0; i < 10; i++) {
      MathProblem problem = new MathProblem();
      problem.generateQuestion();
      problems[i] = problem;
    }
   
    // Present all the problems to player one by one and capture the player answer

    // Print the result.
   
  }
}